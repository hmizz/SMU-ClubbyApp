export interface Event {
    id : string;
    title: string;
    organizer: string;
    date : string;
    location: string;
      // the club organizer
    description : string; 
    topic: string;
}